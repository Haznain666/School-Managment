{
  "name": "sms-platform-functions",
  "version": "0.1.0",
  "private": true,
  "description": "Firebase Functions (2nd gen) for the SMS Platform — scaffold only in Sprint 1.",
  "main": "lib/index.js",
  "type": "commonjs",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "firebase-admin": "^13.5.0",
    "firebase-functions": "^6.5.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
